// Boolean data type in TypeScript

const isActive : boolean = true;
const isLoggedIn : boolean = false;
const isVerified : boolean = true;
const isAdmin : boolean = false; 
const isOnline : boolean = true;
const isAvailable : boolean = false;
const isSubscribed : boolean = true; 

console.log(`Is Active: ${isActive}`);
console.log(`Is Logged In: ${isLoggedIn}`);
console.log(`Is Verified: ${isVerified}, Is Admin: ${isAdmin}, Is Online: ${isOnline}, Is Available: ${isAvailable}, Is Subscribed: ${isSubscribed}`);
